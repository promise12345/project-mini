<view class="input-box" catch:tap="handleCancel">
	<view class="box-wrap" catch:tap="() => void">
		<view class="box-title">请输入您的内容</view>
		<view class="box-content">
			<input value="{{newNum}}" focus bindblur="handleBlur" />
		</view>
		<view class="box-footer">
			<button type="primary" size="mini" catch:tap="handleOk">确定</button>
		</view>
	</view>
</view>

	<!-- <comp-modal
	modal-title="请输入您的内容hhh"
	bind:onCancel="handleCancel"
	bind:onConfirm="handleOk"
	modal-visible="{{boxVisible}}">
		<input value="{{newNum}}" focus bindblur="handleBlur" />
	</comp-modal> -->
  
